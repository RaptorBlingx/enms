<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EnMS - Energy Management System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            flex: 1;
        }

        header {
            text-align: center;
            padding: 40px 20px;
            color: white;
        }

        header h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }

        .status-bar {
            background: rgba(255,255,255,0.95);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }

        .status-bar h2 {
            font-size: 1.3rem;
            margin-bottom: 15px;
            color: #667eea;
        }

        .status-items {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
        }

        .status-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .status-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #10b981;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        .status-item span {
            font-size: 0.95rem;
            color: #555;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        .service-card {
            background: white;
            border-radius: 12px;
            padding: 30px;
            text-decoration: none;
            color: #333;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
        }

        .service-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.25);
        }

        .service-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .service-card h3 {
            font-size: 1.4rem;
            margin-bottom: 10px;
            color: #667eea;
        }

        .service-card p {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .service-badge {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            margin-top: 10px;
            font-weight: 600;
        }

        footer {
            background: rgba(255,255,255,0.1);
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: 40px;
        }

        footer p {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .quick-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .stat-card {
            background: rgba(255,255,255,0.95);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.15);
        }

        .stat-card h3 {
            color: #667eea;
            font-size: 2rem;
            margin-bottom: 5px;
        }

        .stat-card p {
            color: #666;
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            header h1 {
                font-size: 2rem;
            }

            .services-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>⚡ EnMS</h1>
            <p>Energy Management System - Real-time Factory Intelligence</p>
        </header>

        <div class="status-bar">
            <h2>🟢 System Status</h2>
            <div class="status-items">
                <div class="status-item">
                    <div class="status-indicator"></div>
                    <span>Data Pipeline</span>
                </div>
                <div class="status-item">
                    <div class="status-indicator"></div>
                    <span>Database</span>
                </div>
                <div class="status-item">
                    <div class="status-indicator"></div>
                    <span>Analytics</span>
                </div>
                <div class="status-item">
                    <div class="status-indicator"></div>
                    <span>Simulator</span>
                </div>
            </div>
        </div>

        <div class="quick-stats">
            <div class="stat-card">
                <h3 id="machines-count">7</h3>
                <p>Active Machines</p>
            </div>
            <div class="stat-card">
                <h3 id="data-points">130+</h3>
                <p>Data Points/Hour</p>
            </div>
            <div class="stat-card">
                <h3 id="uptime">99.9%</h3>
                <p>System Uptime</p>
            </div>
        </div>

        <div class="services-grid">
            <!-- Grafana Dashboard -->
            <a href="/grafana/" class="service-card">
                <div class="service-icon">📊</div>
                <h3>Grafana Dashboards</h3>
                <p>Real-time visualization of energy consumption, production metrics, and environmental data across all factory machines.</p>
                <span class="service-badge">ANALYTICS</span>
            </a>

            <!-- Node-RED Editor -->
            <a href="/nodered/" class="service-card">
                <div class="service-icon">🔄</div>
                <h3>Node-RED Pipeline</h3>
                <p>Visual flow editor for the data ingestion pipeline. Configure MQTT subscriptions, data transformations, and database operations.</p>
                <span class="service-badge">DATA PIPELINE</span>
            </a>

            <!-- Analytics API -->
            <a href="/api/analytics/docs" class="service-card">
                <div class="service-icon">🤖</div>
                <h3>Analytics API</h3>
                <p>Machine learning models for anomaly detection, forecasting, and energy optimization. RESTful API with interactive documentation.</p>
                <span class="service-badge">ML/AI</span>
            </a>

            <!-- Query Service -->
            <a href="/api/query/docs" class="service-card">
                <div class="service-icon">🔍</div>
                <h3>Query Service</h3>
                <p>High-performance query API for OVOS voice assistant integration. Natural language queries for energy data and insights.</p>
                <span class="service-badge">VOICE API</span>
            </a>

            <!-- Simulator Control -->
            <a href="/api/simulator/docs" class="service-card">
                <div class="service-icon">🏭</div>
                <h3>Factory Simulator</h3>
                <p>Realistic factory machine simulator with 7 machines generating energy, production, and environmental data for testing.</p>
                <span class="service-badge">TESTING</span>
            </a>
        </div>
    </div>

    <footer>
        <p>EnMS v1.0 | Energy Management System | Built with ❤️ for sustainable manufacturing</p>
        <p style="margin-top: 10px; font-size: 0.8rem;">
            <a href="https://github.com/yourusername/enms" style="color: white; text-decoration: none;">📚 Documentation</a> |
            <a href="/health" style="color: white; text-decoration: none;">🏥 Health Check</a>
        </p>
    </footer>

    <script>
        // Simple animations and dynamic updates
        document.addEventListener('DOMContentLoaded', function() {
            // Simulate live data updates
            const updateStats = () => {
                const dataPoints = document.getElementById('data-points');
                const current = parseInt(dataPoints.textContent);
                dataPoints.textContent = (current + Math.floor(Math.random() * 10)) + '+';
            };

            // Update stats every 30 seconds
            setInterval(updateStats, 30000);

            // Add smooth scroll
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });
        });
    </script>
</body>
</html>